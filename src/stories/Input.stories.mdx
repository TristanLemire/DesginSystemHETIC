import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Input } from "../components/atoms/Input";

<Meta
  title="Components/Atoms/Input"
  component={Input}
  argTypes={{
    placeholder: {
      control: {
        type: "text",
      },
    },
    type: {
      control: {
        type: "radio",
        options: ["email", "password", "text", "tel"],
      },
    },
  }}
  parameters={{
    design: {
      type: "figma",
      url:
        "https://www.figma.com/file/Ngi8PyV1EngqKeQyHwMDl4/Design-System?node-id=1%3A5",
    },
  }}
/>

# Input

export const Template = (args) => <Input {...args} />;

<Canvas>
  <Story
    name="Email input"
    args={{
      type: "email",
      placeholder: "email@example.com",
      callback: () => {},
    }}
  >
    {Template.bind({})}
  </Story>
  <Story
    name="Password input"
    args={{ type: "password", placeholder: "Password", callback: () => {} }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
